using addressbook.Entities;
using System.Linq;

namespace AddressBook.Helper
{
    public class KeyMapper
    {
        public string AddKeys(User userData)
        {
            User updatedData = userData;

            //updatedData.Addresses.Select(address =>
            //{
                
            //}).ToList();


            return "yes";
        }
    }
}
